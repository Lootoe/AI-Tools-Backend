services:
  api:
    build: .
    ports:
      - "8686:3000"
    environment:
      - NODE_ENV=production
      - AI_API_BASE_URL=${AI_API_BASE_URL}
      - AI_API_KEY=${AI_API_KEY}
      - CORS_ORIGIN=${CORS_ORIGIN}
    restart: unless-stopped
